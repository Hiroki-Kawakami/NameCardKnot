# My Card import flow: import a .mnc.pdf into the mycard flash partition, then
# confirm the Home screen reflects it (preview + name raster). Start from no
# saved card: rm simulator/mycard.img before running for the empty path.
# tap: panel_x = logical_y, panel_y = 540 - logical_x.
settle
tap 390 270                                       # "No My Card" / My Card -> import browser
settle
tap 372 270                                       # root row 4: yamada.mnc.pdf -> import
wait 10000
settle
capture simulator/verify/out/mycard_home.jpg      # Home with the saved card
tap 388 425                                        # tap the card -> open from cache (MMIO)
settle
capture simulator/verify/out/mycard_namecard.jpg
quit
